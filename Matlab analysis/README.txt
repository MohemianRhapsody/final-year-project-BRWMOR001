The waveform plotting MATLAB code contains sections of code used to plot the waveforms for my final year report.

The code must be in the same folder (or along the same path) as the audio samples that are to be plot.